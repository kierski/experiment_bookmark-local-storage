<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title> <%= title %> </title>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <link rel="stylesheet" href="css/main.css">
  </head>
  <body id="<%= title %>">

    <nav class="navbar navbar-toggleable-md navbar-light bg-faded">
      <div class="container">

          <% if (title === 'panel') { %>
            <a class="navbar-brand" href="/bookmark_panel">Add bookmark</a>
          <% } else { %>
            <a class="navbar-brand" href="/">Mark</a>
          <% } %>

        <ul class="navbar-nav">
          <li class="nav-item">
            <a class="nav-link" href="/">home</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/favourite">favourite</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/youtube">youtube</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/web">web</a>
          </li>
        </ul>
      </div>
    </nav>

    <% if (title === 'panel') { %>
      <% include bookmark_panel %>
    <% } else { %>
      <% include jumbotron %>
    <% } %>
